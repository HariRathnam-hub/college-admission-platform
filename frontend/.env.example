# Base URL of the deployed backend API (include /api/v1).
# Leave unset for local development — the Vite dev server proxies /api to http://localhost:5000.
VITE_API_BASE_URL=https://your-backend.onrender.com/api/v1
